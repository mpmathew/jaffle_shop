jaffle_shop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: "{{env_var('POSTGRES_USER')}}"
      password: "{{env_var('POSTGRES_USER')}}"
      port: 5432
      dbname: postgres
      schema: public
      threads: 4
  
