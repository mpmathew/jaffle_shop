jaffle_shop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: "{{env_var('AIRFLOW_POSTGRES_USER')}}"
      password: testpassword
      port: 5432
      dbname: postgres
      schema: public
      threads: 4
  
