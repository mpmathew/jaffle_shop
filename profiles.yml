jaffle_shop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: postgres
      password: "{{env_var('POSTGRES_PASSWORD_POC')}}"
      port: 5432
      dbname: postgres
      schema: public
      threads: 4
  
