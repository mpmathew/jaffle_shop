jaffle_shop:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: "{{env_var('TEST_POSTGRES_USER')}}"
      password: "{{env_var('TEST_POSTGRES_PASSWORD')}}"
      port: 5432
      dbname: postgres
      schema: public
      threads: 4
  
